{% extends 'base.html.twig' %}

{% block title %}Typebiere{% endblock %}

{% block body %}
	<div class="container">
		<div class="row justify-content-center m-5">

			<div class="card bg-dark border rounded-5 border mt-4 col-md-8 justify-center content-center">
				<div class="card-title text-center text-white">
					<h1>Type de bière</h1>
					<div class="card-body text-center">
						<table class="table table-dark table-hover">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ typebiere.idType }}</td>
            </tr>
            <tr>
                <th>Nom Type de bière</th>
                <td>{{ typebiere.nomType }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-warning" role="button" href="{{ path('app_typebiere_index') }}">Retour à la liste</a>

    <a class="btn btn-info" role="button" href="{{ path('app_typebiere_edit', {'idType': typebiere.idType}) }}">Modifier</a>

    {{ include('typebiere/_delete_form.html.twig') }}

    					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
