{% extends 'base.html.twig' %}

{% block title %}Continent
{% endblock %}

{% block body %}
	<div class="container-fluid">
		<div class="row justify-content-center m-5">

			<div class="card bg-dark border rounded-5 border mt-4 col-md-8 justify-center content-center">
				<div class="card-title text-center text-white">
					<h1>Continent</h1>
					<div class="card-body text-center">
						<table class="table table-dark table-hover">
							<tbody>
								<tr>
									<th>IdContinent</th>
									<td>{{ continent.idContinent }}</td>
								</tr>
								<tr>
									<th>NomContinent</th>
									<td>{{ continent.nomContinent }}</td>
								</tr>
							</tbody>
						</table>

						<a class="btn btn-warning" role="button" href="{{ path('app_continent_index') }}">Retour à la liste</a>

						<a class="btn btn-info" role="button" href="{{ path('app_continent_edit', {'idContinent': continent.idContinent}) }}">Modifier</a>

						{{ include('continent/_delete_form.html.twig') }}
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
