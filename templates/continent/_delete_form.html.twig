<form method="post" action="{{ path('app_continent_delete', {'idContinent': continent.idContinent}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ continent.idContinent) }}">
    <button class="btn btn-danger" id="submitForm" >{% trans %}Supprimer{% endtrans %}</button>
</form>

