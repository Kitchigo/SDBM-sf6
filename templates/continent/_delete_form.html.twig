<form method="post" action="{{ path('app_continent_delete', {'idContinent': continent.idContinent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ continent.idContinent) }}">
    <button class="btn">Delete</button>
</form>
