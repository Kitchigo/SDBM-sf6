{% extends 'base.html.twig' %}

{% block title %}Edit Fabricant
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row justify-content-center m-5">

			<div class="card bg-dark border rounded-5 border mt-4 col-md-8 justify-center content-center">
				<div class="card-title text-center text-white">
					<h1>Modification du Fabicant</h1>
				</div>

				<div class="card-body text-center">

					{{ include('fabricant/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

					<a class="btn btn-warning mt-3" role="button" href="{{ path('app_fabricant_index') }}">Retour à la liste</a>

					{{ include('fabricant/_delete_form.html.twig') }}

				</div>

			</div>

		</div>
	</div>
{% endblock %}
