{% extends 'base.html.twig' %}

{% block title %}Ticket{% endblock %}

{% block body %}
    <h1>Ticket</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Annee</th>
                <td>{{ ticket.annee }}</td>
            </tr>
            <tr>
                <th>NumeroTicket</th>
                <td>{{ ticket.numeroTicket }}</td>
            </tr>
            <tr>
                <th>DateVente</th>
                <td>{{ ticket.dateVente ? ticket.dateVente|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ticket_index') }}">back to list</a>

    <a href="{{ path('app_ticket_edit', {'annee': ticket.annee}) }}">edit</a>

    {{ include('ticket/_delete_form.html.twig') }}
{% endblock %}
