<form method="post" action="{{ path('app_couleur_delete', {'idCouleur': couleur.idCouleur}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ couleur.idCouleur) }}">
    <button class="btn btn-danger" id="submitForm">{% trans %}Supprimer{% endtrans %}</button>
</form>

{# onsubmit="return confirm('Are you sure you want to delete this item?');" #}