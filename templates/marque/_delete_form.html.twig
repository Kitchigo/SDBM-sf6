<form method="post" action="{{ path('app_marque_delete', {'idMarque': marque.idMarque}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marque.idMarque) }}">
    <button class="btn">Delete</button>
</form>
