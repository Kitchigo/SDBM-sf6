<form method="post" action="{{ path('app_marque_delete', {'idMarque': marque.idMarque}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marque.idMarque) }}">
    <button class="btn btn-danger" id="submitForm">{% trans %}Supprimer{% endtrans %}</button>
</form>
